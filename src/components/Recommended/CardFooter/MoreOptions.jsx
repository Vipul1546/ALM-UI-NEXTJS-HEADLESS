import PropTypes from "prop-types";

const MoreOptions = ({ startCtaLabel, exploreCtaLabel }) => {
    return null;
}

export default MoreOptions;

MoreOptions.propTypes = {
    startCtaLabel: PropTypes.string.isRequired
}